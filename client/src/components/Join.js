import React from 'react';
import { Typography } from '@material-ui/core';

const Join = () => {
    return (
        <div>
            <Typography variant='h1'>Join</Typography>
        </div>
    );
};

export default Join;
