import React from 'react';
import { Typography } from '@material-ui/core';

const Chat = () => {
    return (
        <div>
            <Typography variant='h1'>Chat</Typography>
        </div>
    );
};

export default Chat;
